import { memo } from "react";

const CreateNote = memo(function CreateNote() {
  return (
    <div>
      <div>CreateNote</div>
      <div>CreateNote</div>
      <div>CreateNote</div>
    </div>
  );
});

export default CreateNote;
